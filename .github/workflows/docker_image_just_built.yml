name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        docker build . --file Dockerfile --tag my_docker_image:$(date +%s)
        docker scan --login --token ${{ secrets.SNYK_TOKEN }}
        docker scan --severity=medium my_docker_image:$(date +%s)
